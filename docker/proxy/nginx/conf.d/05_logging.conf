# Log to container stdout/stderr so docker compose logs can collect them.
# "main" log_format is defined in the default nginx.conf included in official images.

access_log /dev/stdout main;
error_log  /dev/stderr notice;