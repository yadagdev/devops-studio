server {
  listen 80;

  default_type text/plain;

  # devops-proxy 自体の生存確認
  location / {
    return 200 "devops-proxy ok\n";
  }

  location = /healthz {
    return 200 "ok\n";
  }

  # アプリ別の location を読み込む
  include /etc/nginx/conf.d/apps/*.conf;
}
